import ProfileCard from "./components/ProfileCard";

const App = () => {
  return (
    <div className="bg-gradient-to-b from-green-300 to-cyan-300">
      <div className="flex flex-col items-center justify-center h-screen">
        <ProfileCard />
      </div>
    </div>
  );
};

export default App;
